AWSTemplateFormatVersion: '2010-09-09'
Description: 'CI/CD Role for GitHub Actions deployment'

Parameters:
  GitHubOrg:
    Type: String
    Description: GitHub organization or username
    Default: dutchovenbread
  
  GitHubRepo:
    Type: String
    Description: GitHub repository name
    Default: ansiblelab

  GitHubThumbprint1:
    Type: String
    Description: Thumbprint for GitHub OIDC provider
    Default: 6938fd4d98bab03faadb97b34396831e3780aea1
  
  GitHubThumbprint2:
    Type: String
    Description: Secondary Thumbprint for GitHub OIDC provider
    Default: 1c58a3a8518e8759bf075b76b750d4f2df264fcd

Resources:
  GitHubOIDCProvider:
    Type: AWS::IAM::OIDCProvider
    Properties:
      Url: https://token.actions.githubusercontent.com
      ClientIdList:
        - sts.amazonaws.com
      ThumbprintList:
        - !Ref GitHubThumbprint1
        - !Ref GitHubThumbprint2

  GitHubActionsRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: GitHubActionsDeploymentRole
      Description: Role assumed by GitHub Actions for deployment
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: !GetAtt GitHubOIDCProvider.Arn
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                token.actions.githubusercontent.com:aud: sts.amazonaws.com
              StringLike:
                token.actions.githubusercontent.com:sub: !Sub repo:${GitHubOrg}/${GitHubRepo}:*
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/PowerUserAccess
      Policies:
        - PolicyName: IAMPermissions
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - iam:CreatePolicy
                  - iam:DeletePolicy
                  - iam:GetPolicy
                  - iam:ListPolicies
                  - iam:ListPoliciesGrantingServiceAccess
                  - iam:CreateRole
                  - iam:DeleteRole
                  - iam:GetRole
                  - iam:PassRole
                  - iam:AttachRolePolicy
                  - iam:DetachRolePolicy
                  - iam:PutRolePolicy
                  - iam:DeleteRolePolicy
                  - iam:GetRolePolicy
                  - iam:CreateInstanceProfile
                  - iam:DeleteInstanceProfile
                  - iam:GetInstanceProfile
                  - iam:AddRoleToInstanceProfile
                  - iam:RemoveRoleFromInstanceProfile
                  - iam:*Tag*
                Resource: '*'

Outputs:
  RoleArn:
    Description: ARN of the GitHub Actions deployment role
    Value: !GetAtt GitHubActionsRole.Arn
    Export:
      Name: GitHubActionsRoleArn